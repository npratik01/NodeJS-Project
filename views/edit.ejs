<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen p-10 bg-zinc-900 text-white">
        <div class="flex justify-end w-full">
            <a href="/logout" class="px-3 py-2 bg-red-500 text-black rounded-md">Logout</a>
        </div>
        <h1 class="text-3xl"><span class="text-yellow-500"></span> <%= post.user.name %> wants edits ?...</h1>
        <h5>Edit Post !</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content" class="p-3 rounded-md resize-none bg-zinc-900 border-2 border-zinc-500 w-1/3 mt-3 outline-none block mb-3"><%= post.content %></textarea>
            <input type="submit" name="" class="w-40 p-2 bg-yellow-500 text-black rounded-md block mb-10" value="Update Post">
            <hr>
        </form>
    </div>
</body>
</html>